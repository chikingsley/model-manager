{
  "name": "mm-tui",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.tsx",
    "start": "bun run src/index.tsx",
    "test": "bun test",
    "build": "bun run build.ts",
    "install-bin": "cp mm-tui ~/.local/bin/"
  },
  "dependencies": {
    "@opentui/core": "latest",
    "@opentui/solid": "latest",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.8"
  }
}
